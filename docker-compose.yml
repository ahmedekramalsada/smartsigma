version: '3.8'

services:
  spring-api:
    image: ${DOCKER_IMAGE:-spring-api:latest}
    container_name: spring-api
    restart: always
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod